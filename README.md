# tBank Cooler - Web приложение

Современное веб-приложение для взаимодействия с API tBank. Проект построен на React 19 с использованием TypeScript, Material-UI и других современных технологий.

## 🚀 Особенности

- **Аутентификация и авторизация** - система логина и регистрации с защитой маршрутов
- **Чат с поддержкой Markdown** - интерактивный чат с возможностью отправки сообщений и рендерингом Markdown
- **Профиль пользователя** - страница с информацией о пользователе
- **Тарифные планы** - отображение доступных тарифов
- **Красивый UI** - Material-UI компоненты с кастомной темой
- **TypeScript** - полная типизация кода для лучшей разработки
- **ESLint + Prettier** - автоматическое форматирование и проверка кода
- **Vite** - современный и быстрый сборщик проектов

## 📋 Требования

- Node.js 16+ (рекомендуется 18+)
- npm или yarn

## 📦 Установка

1. Клонируйте репозиторий:

```bash
git clone https://github.com/PirojochecGG/hakTbank.git
cd hakTbank/web
```

2. Установите зависимости:

```bash
npm install
```

3. Создайте файл `.env` в корне папки `web` (если его еще нет):

```env
VITE_API_BASE_URL=https://api-tbank-hack.tagan.fullhouseit.ru
```

## 🛠️ Доступные команды

### Разработка

Запуск dev-сервера на `http://localhost:5173`:

```bash
npm run dev
```

### Сборка для продакшена

```bash
npm run build
```

### Превью собранного проекта

```bash
npm run preview
```

### Проверка кода (ESLint)

```bash
npm run lint
```

### Форматирование кода (Prettier)

```bash
npm run format
```

## 📁 Структура проекта

```
web/
├── src/
│   ├── api/
│   │   └── api.ts              # Конфигурация и методы для работы с API
│   ├── components/
│   │   ├── Layout.tsx          # Основной макет приложения
│   │   ├── MarkdownRenderer.tsx # Компонент для рендеринга Markdown
│   │   ├── RequireAuth.tsx      # HOC для защиты маршрутов
│   │   └── Auth/
│   │       ├── LoginForm.tsx    # Форма входа
│   │       └── RegisterForm.tsx # Форма регистрации
│   │   └── Common/
│   │       └── TButton.tsx      # Переиспользуемый компонент кнопки
│   ├── context/
│   │   └── AuthContext.tsx      # Контекст для управления аутентификацией
│   ├── pages/
│   │   ├── ChatPage.tsx         # Страница чата
│   │   ├── LoginPage.tsx        # Страница входа
│   │   ├── RegisterPage.tsx     # Страница регистрации
│   │   ├── ProfilePage.tsx      # Страница профиля пользователя
│   │   └── TariffsPage.tsx      # Страница с тарифами
│   ├── utils/
│   │   └── chatStorage.ts       # Утилиты для работы с хранилищем чата
│   ├── App.tsx                  # Главный компонент приложения
│   ├── main.tsx                 # Точка входа
│   ├── index.css                # Глобальные стили
│   └── theme.ts                 # Material-UI тема
├── index.html                   # HTML шаблон
├── package.json                 # Зависимости и скрипты
├── tsconfig.json                # Конфиг TypeScript
├── tsconfig.app.json            # Конфиг TypeScript для приложения
├── tsconfig.node.json           # Конфиг TypeScript для Vite
├── vite.config.ts               # Конфиг Vite
├── eslint.config.js             # Конфиг ESLint
├── .prettierrc                   # Конфиг Prettier
└── .env                          # Переменные окружения
```

## 🔐 Маршруты приложения

| Маршрут         | Описание             | Требует аутентификации |
| --------------- | -------------------- | ---------------------- |
| `/login`        | Страница входа       | ❌ Нет                 |
| `/register`     | Страница регистрации | ❌ Нет                 |
| `/user/profile` | Профиль пользователя | ✅ Да                  |
| `/chat`         | Чат                  | ✅ Да                  |
| `/tariffs`      | Тарифные планы       | ✅ Да                  |

## 🔌 API интеграция

Приложение взаимодействует с API через Axios. Базовый URL берется из переменной окружения `VITE_API_BASE_URL`.

Основной файл для работы с API: `src/api/api.ts`

## 🎨 Технологический стек

### Frontend Фреймворки

- **React 19** - основной фреймворк
- **React Router DOM 7** - маршрутизация
- **TypeScript** - типизация кода

### UI библиотеки

- **Material-UI (MUI) 7** - компоненты интерфейса
- **MUI Icons Material** - иконки
- **Emotion** - CSS-in-JS решение

### Инструменты разработки

- **Vite** - быстрый сборщик и dev-сервер
- **ESLint** - проверка кода
- **Prettier** - форматирование кода
- **TypeScript Compiler** - компилирование TypeScript

### HTTP клиент

- **Axios** - работа с API запросами

## 🧪 Тестирование и качество кода

Перед отправкой кода в репозиторий:

1. Проверьте код на ошибки:

```bash
npm run lint
```

2. Отформатируйте код:

```bash
npm run format
```

3. Убедитесь, что приложение собирается без ошибок:

```bash
npm run build
```

## 📚 Основные компоненты

### AuthContext

Управляет состоянием аутентификации пользователя. Предоставляет информацию о статусе входа и данные пользователя.

### Layout

Основной макет приложения, содержит навигацию и основной контент.

### RequireAuth

Higher-Order Component для защиты маршрутов. Перенаправляет незавершенных пользователей на страницу входа.

### MarkdownRenderer

Компонент для отображения Markdown текста в чате.

## 🌍 Переменные окружения

Приложение использует следующие переменные окружения:

- `VITE_API_BASE_URL` - базовый URL API сервера (обязательная)

Создайте файл `.env` в корне папки `web` для локальной разработки.

## 📖 Дополнительная информация

- Проект использует Babel React Compiler для оптимизации производительности
- Все компоненты написаны как функциональные компоненты с хуками
- CSS стили управляются через Material-UI theming и Emotion

## 🤝 Разработка

При разработке новых фич:

1. Создавайте новые компоненты в папке `components`
2. Добавляйте новые страницы в папку `pages`
3. Используйте TypeScript для типизации
4. Следуйте стилю кода (используйте `npm run format`)
5. Убедитесь, что код проходит ESLint проверку

## 📝 Лицензия

Этот проект является частью хакатона CyberGarden2025.

## 📧 Контакты

Для вопросов и предложений создавайте issues в репозитории.

---

**Последнее обновление:** Декабрь 2025
